# Memory Change Log

## 2026-03-14

- Initialized workspace memory structure and baseline documents.
- Completed the first shared `memory/` baseline for `shared`, `dashboard`, and `backend` scopes.
- Deepened the baseline with analysis-backed density notes for major frontend and backend hotspots.
- Captured analyzed Agent Skills materials in `memory/agent-skills/`.
- Added root `skills/` references and the first analysis-backed skill set for shared, dashboard, backend, extensions, and apps scopes.
- Deepened `memory/agent-skills/` app and extension evidence with confirmed route structure, manifest validation rules, backend models, management commands, tasks, and GraphQL surfaces.
- Added analysis-backed surface maps for extension UI/backend routing and app lifecycle operations in `memory/agent-skills/`.
- Deepened the extension surface map with confirmed webhook-management, widget-rendering, and subscription-query handling details.
- Added a direct analysis of the local `skills/bankr-signals/` package pattern and its structural differences from the current root skills.
- Improved all root skills into richer skill packages with local `references/` directories and updated skill metadata to better follow Agent Skills triggering guidance.
- Added `bankr-signals` package files to the machine-readable manifest and aligned the manifest with the upgraded skill-package structure.
- Added a shared dashboard-backend connection map with Mermaid diagrams and an endpoint catalog grounded in confirmed GraphQL, upload, public API, and app-install source paths.
